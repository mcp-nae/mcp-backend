[project]
name = "nae"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-mcp-adapters>=0.1.12",
    "mcp[cli]>=1.20.0",
]
